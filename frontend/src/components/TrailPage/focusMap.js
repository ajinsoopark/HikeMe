import React from 'react'

const FocusMap = ({ latitude, longitude }) => {

    console.log(latitude, longitude)

    return (
        <div className='focusMapContainer'>
            map
        </div>
    )
}

export default FocusMap